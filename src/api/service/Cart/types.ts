export type CartResponse = {
  title: string;
  image: string;
  code: string;
  value: string;
  quantidade: number;
  subTotal: string;
  total: string;
};

export type ItemCode = {
  code: string;
};
